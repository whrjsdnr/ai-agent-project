# 개발 규칙

이 파일은 이 프로젝트에서 작업하는 AI coding agent를 위한 개발 규칙입니다.
프로젝트의 실제 설정과 이 규칙이 충돌할 경우, 실제 프로젝트 설정을 우선합니다.

## Python 및 의존성 관리

- Python 3.12를 사용합니다. 프로젝트의 `.python-version` 및 `pyproject.toml` 설정을 따릅니다.
- Python 실행과 패키지 관리는 `uv`를 우선 사용합니다.
- `pip`를 직접 사용하는 것은 피합니다.

## 코드 품질

- Ruff를 린터와 포매터로 사용합니다.
- 타입 힌트를 적극적으로 작성합니다.
- 불필요하게 복잡한 설계나 코드를 만들지 않습니다.

## 테스트

- pytest를 사용합니다.
- 새로운 기능을 추가할 때는 가능한 한 테스트를 함께 작성합니다.
- 코드 변경 후에는 관련 테스트를 실행합니다.
- 테스트가 실패하면 원인을 분석하고 수정합니다.

## 개발 워크플로

- 코드를 수정하기 전에 현재 프로젝트 구조와 관련 코드를 먼저 확인합니다.
- 큰 변경을 시작하기 전에 구현 계획을 설명합니다.
- 기존 코드를 존중하며, 필요 이상의 파일을 수정하지 않습니다.
- 작업 완료 후 변경 사항을 요약합니다.
- 가능하면 Ruff와 pytest를 실행합니다.

## Git 및 안전한 작업

- 작업 시작 전에 `git status`를 확인합니다.
- 기존 작업물을 임의로 삭제하거나 덮어쓰지 않습니다.
- 사용자의 명시적인 승인 없이는 `git reset --hard`, `git clean -fd` 등 파괴적인 명령을 실행하지 않습니다.
- 사용자의 명시적인 승인 없이는 `sudo` 명령을 실행하지 않습니다.
- 작업 완료 후 사용자가 `git diff`로 확인할 수 있도록 변경 내용을 명확하게 설명합니다.

## 보안

- `.env` 파일 및 비밀키를 읽거나 출력하지 않습니다.
- API 키, 비밀번호, 토큰 등 민감한 정보를 코드에 하드코딩하지 않습니다.
- 시스템 전체에 영향을 줄 수 있는 명령은 실행 전에 사용자에게 확인합니다.
